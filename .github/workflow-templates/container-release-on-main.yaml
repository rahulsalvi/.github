---
name: Container release on push to main

on:
  push:
    branches: [$default-branch]

jobs:
  container-release:
    uses: rahulsalvi/.github/.github/workflows/container-release.yaml@main
    secrets: inherit
